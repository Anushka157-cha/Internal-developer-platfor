# Production Environment Variables
# Copy this file to .env.production and fill in your production values
# DO NOT commit .env.production to git!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Use managed database service in production (AWS RDS, Azure Database, etc.)
DATABASE_HOST=your-db-host.region.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your-strong-database-password-here
DATABASE_NAME=idp_db

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-generated-jwt-secret-here
JWT_EXPIRATION=7d

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Use managed Redis service in production (Redis Labs, AWS ElastiCache, etc.)
REDIS_HOST=your-redis-host.cache.amazonaws.com
REDIS_PORT=6379

# =============================================================================
# EMAIL CONFIGURATION (PRODUCTION)
# =============================================================================
# DO NOT use personal Gmail in production!
# Use professional email service: SendGrid, AWS SES, Mailgun, or Postmark

# Option 1: SendGrid (Recommended)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

# Option 2: AWS SES
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_USER=your-aws-ses-smtp-username
# EMAIL_PASSWORD=your-aws-ses-smtp-password

# Option 3: Mailgun
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USER=postmaster@your-domain.mailgun.org
# EMAIL_PASSWORD=your-mailgun-smtp-password

EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USER=apikey
EMAIL_PASSWORD=your-sendgrid-api-key-here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
PORT=3001
NODE_ENV=production

# Frontend URL - Your production domain
FRONTEND_URL=https://your-production-domain.com

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. Generate strong secrets using the commands in DEPLOYMENT_CHECKLIST.md
# 2. Set up professional email service (SendGrid recommended)
# 3. Use managed database and Redis services
# 4. Set environment variables in your hosting platform
# 5. Enable HTTPS/SSL certificates
# 6. Configure DNS for your domain
# 7. Set up monitoring and logging
# 8. Enable database backups
# 9. Configure rate limiting
# 10. Test all features before going live
